{# Prerender socials items to be provide in the footer.html.twig organisms #}
{% set rendered_socials %}
  {% for social in socials %}
    {{ include('@atoms/social.html.twig', {
      'name': social.name,
      'url': social.url,
      'svg': social.svg
    }) }}
  {% endfor %}
{% endset %}

{# copyright use getThemeSetting() twig func defined in AlsBaseExtension #}
{{ include('@organisms/footer.html.twig', {
  'title': site_name,
  'copyright': theme_setting('copyright'),
  'menu': content,
  'social': rendered_socials
}) }}
